# Server Configuration
server:
  port: 8761

# Spring Application Configuration
spring:
  application:
    name: service-discovery

# Eureka Server Configuration
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true

  client:
    # This is a server, so don't register itself
    register-with-eureka: false
    fetch-registry: false

  server:
    # Disable self-preservation for development (allows quick removal of dead services)
    enable-self-preservation: false
    # How often to evict expired services (milliseconds)
    eviction-interval-timer-in-ms: 60000

# Actuator endpoints for health monitoring
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always