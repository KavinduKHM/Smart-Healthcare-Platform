# Keep the Docker build context minimal and deterministic.
# This image copies the pre-built JAR from target/.

# Ignore everything by default
**

# Keep Docker build files
!Dockerfile
!.dockerignore

# Keep the built JAR
!target/
!target/*.jar